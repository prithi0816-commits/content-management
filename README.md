PROJECT NM - Contact Manager / ConnectHub
Project Overview
ConnectHub is a backend web app to manage contacts in one place. Users can store, search, and categorize contacts securely.

Completed Features
Express server setup
MongoDB connection (partial)
Basic routes and CRUD structure
User authentication setup (JWT + bcrypt)
Pending / To-Do
Complete CRUD operations
Frontend integration
Full error handling and validations
Folder Structure
server.js – main backend server
config/db.js – MongoDB connection
contact-manager/ – sub-project with package.json and node_modules
routes/ – API endpoints
controllers/ – business logic
models/ – Mongoose schemas
.env – environment variables (not included)
.gitignore – files to ignore
README.md – project overview
How to Run
Install dependencies: npm install
Create .env file with:
